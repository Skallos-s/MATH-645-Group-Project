% Make a Dialog Box
% NOTE: Text can be outside of the dialog box, so make it big enough to fit
% in the box properly.
function result = makeDialogBox(text, width, height, pointingAngle, pointingSize, textColor, fontSize)
    obj = makeRectangle(width, height);
    % Scalene triangle Will do Later

    %text(x, y, 'Color', textColor, 'FontSize', fontSize);
end